import { objectModel } from "../database/model"
import { v4 as uuidv4 } from "uuid"
import minio from "./connection"

export const upload = (payload: File, parent: string | null, owner: string) => {

}